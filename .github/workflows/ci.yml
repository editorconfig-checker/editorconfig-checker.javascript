name: ci

on: [push]

jobs:
  build_and_lint:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v23
      - run: npm ci
      - run: npm start
      - run: npm run lint
